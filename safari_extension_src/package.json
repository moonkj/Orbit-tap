{
  "name": "swift-safari-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.2",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "rollup": "^4.30.1",
    "rollup-plugin-css-only": "^4.5.2",
    "typescript": "^5.7.3",
    "tslib": "^2.8.1",
    "vitest": "^3.0.4"
  }
}
